services:
  # Proxy CORS para evitar errores de CORS al conectar con GCP
  cors-proxy:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    container_name: rag-cors-proxy
    networks:
      - rag-network

networks:
  rag-network:
    driver: bridge
